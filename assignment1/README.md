# FastAPI Todo CRUD API

A lightweight, in-memory RESTful CRUD API built with Python and FastAPI for managing task items.

---

## Setup & Running

## Setup & Running

1. **Clone the repository:**
   ```powershell
   git clone https://github.com/WarArha/FLY-RANK-assignment1.git
   cd FLY-RANK-assignment1

## Set up virtual environment & install dependencies:
    python -m venv venv
    .\venv\Scripts\Activate.ps1
    pip install fastapi uvicorn pydantic

## Run the API server:
    uvicorn crud_api:appi --reload